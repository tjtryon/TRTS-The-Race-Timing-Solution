# TRTS (The Race Timing Solution) - Complete Requirements
# Covers Console, GUI, Web, and Data Applications
# Generated: July 27, 2025

# Core dependencies for all applications
sqlite3==3.8.0+  # Built-in with Python 3.x, but ensuring compatibility

# Console Application Dependencies
bcrypt>=4.0.0
playsound>=1.3.0

# GUI Application Dependencies (GTK4 + Python)
PyGObject>=3.42.0
pycairo>=1.20.0

# Web Application Dependencies (Flask)
Flask>=2.3.0
Werkzeug>=2.3.0
Jinja2>=3.1.0
MarkupSafe>=2.1.0
itsdangerous>=2.1.0
click>=8.1.0

# Data Processing Dependencies
# (Currently using built-in csv module, but adding common data libraries)
pandas>=1.5.0
numpy>=1.24.0

# Development and Testing Dependencies
pytest>=7.4.0
pytest-flask>=1.2.0

# Security and Authentication (shared across applications)
# bcrypt already listed above for console
passlib>=1.7.4

# File and System Operations
pathlib2>=2.3.7; python_version < "3.4"

# Audio/Sound Support (for finish beeps)
# playsound already listed above
pygame>=2.5.0  # Alternative sound library

# Date/Time Handling Enhancement
python-dateutil>=2.8.0

# Optional: Enhanced CSV handling
openpyxl>=3.1.0  # For Excel file support
xlrd>=2.0.0      # For older Excel files

# Optional: Enhanced Logging
colorlog>=6.7.0

# Optional: Configuration Management
python-dotenv>=1.0.0

# Optional: Enhanced CLI for console version
click>=8.1.0
rich>=13.4.0  # For enhanced console output

# Platform-specific dependencies
# For Windows audio support
pywin32>=306; sys_platform == "win32"

# For Linux audio support  
pyaudio>=0.2.11; sys_platform == "linux"

# Production Web Server (alternative to Flask dev server)
gunicorn>=21.2.0
waitress>=2.1.0

# Optional: API capabilities
flask-restful>=0.3.10
flask-cors>=4.0.0

# Optional: Enhanced templating and static files
flask-assets>=2.0.0

# Database utilities and migrations
alembic>=1.11.0  # For database migrations if needed

# Optional: Monitoring and logging for web app
flask-logging>=0.1.0

# Development tools
black>=23.7.0     # Code formatting
flake8>=6.0.0     # Linting
mypy>=1.5.0       # Type checking